# Package 'chip'
#To make drawing by R more easy!Printing Style!

#you can install it like this:

rm(list = ls())
if (is.element('chip', installed.packages()[,1]) == FALSE) 
{
  if(is.element('devtools', installed.packages()[,1]) == FALSE)
  {	
    install.packages("devtools")	
    library(devtools)
  }
  else if(is.element('devtools', installed.packages()[,1]) == TRUE){
    library(devtools)
  }
  install_github("youngyaoguai/Rdraw/chip0.1/chip")
  library(chip)
 } else if(is.element('chip', installed.packages()[,1]) == TRUE)
    {
    library(chip)
   }

#in 'chip' package,you can use eight function to draw your picture.you can get help like this:

help(acchist)

help(bonbon)

help(boxeasy)

help(densline)

help(easyline)

help(histeasy)

help(linepoint)

help(pyramid)
